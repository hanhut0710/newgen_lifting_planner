import React from 'react';

export const Bolt = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>bolt</span>
);

export const Fire = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>local_fire_department</span>
);

export const DonutLarge = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>donut_large</span>
);

export const Calculate = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>calculate</span>
);
